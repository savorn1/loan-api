# See auth-service's copy of this file for why this exists — StorageAutoConfiguration
# (common-storage) only wires FileStorageService when S3_ENDPOINT is set, and
# CustomerServiceImpl's constructor needs it.
S3_ENDPOINT=http://localhost:9000
S3_BUCKET=test-bucket
